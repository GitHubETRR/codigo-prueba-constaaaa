#include <stdio.h>
#include <math.h>

int main(){
    float r, a;
    printf("ingrese el valor del radio: ");
    scanf("%f",&r);
    a=3.14*r*r;
    printf("el valor del area del circulo es de: %f", a);
}